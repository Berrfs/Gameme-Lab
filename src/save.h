/* save.h — Save / load system interface (planned feature).
   Currently declares stub functions to satisfy the linker.
   Code updated by Louis, at 06:05PM 2026/03/13 */

#ifndef SAVE_H
#define SAVE_H

/* Stub declarations — implementations pending */
void SaveGame(void);  /* Persist current game state to disk */
void LoadGame(void);  /* Restore game state from disk */

#endif